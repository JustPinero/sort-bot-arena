# Frontend production build — `pnpm build` (Vercel deploys).
# Targets our deployed backend on Railway, which fans out to sort-bot-api.
VITE_API_BASE_URL=https://sort-bot-arena-server-production.up.railway.app
VITE_USE_MOCKS=false
