---
description: Performance audit — bundle size, Lighthouse, render perf. Run via the optimize skill.
---

Invoke the `optimize` skill. Run `pnpm build`, inspect bundle output, profile against the kickoff's per-route budgets (200/250/400KB), and produce a findings report at `audits/optimize-<timestamp>.md`.
