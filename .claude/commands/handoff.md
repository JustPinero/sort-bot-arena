---
description: Write a session handoff doc that lets the next agent pick up cold. Output: audits/handoff-<timestamp>.md.
---

Invoke the `session-handoff` skill. Capture: state of the world (branch, last commit, current phase), what just finished, what's next, known landmines, open questions, and how to verify state. Write to `audits/handoff-<timestamp>.md`.
