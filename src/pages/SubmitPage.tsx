import { PlaceholderPage } from './PlaceholderPage';

export default function SubmitPage() {
  return (
    <PlaceholderPage
      title="Register Your Fighter"
      phase={5}
      description="Monaco editor + multipart upload + live SSE evaluation feedback."
    />
  );
}
